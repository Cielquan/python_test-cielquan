---
name: CodeQL Analysis (Skip)

on: # yamllint disable-line rule:truthy
  pull_request:
    branches:
      - main
    paths-ignore:
      - "**.py"

jobs:
  analyze:
    name: Analyze with CodeQL
    runs-on: ubuntu-latest
    steps:
      - run: exit 0
