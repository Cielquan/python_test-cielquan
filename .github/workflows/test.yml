name: Test workflowception

on:
  workflow_run:
    workflows: ["Test init"]
#     branches: [master]
    types: 
      - completed

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: echo Hi
